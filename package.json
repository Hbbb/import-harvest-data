{
	"name": "harvest-data",
	"version": "1.0.0",
	"description": "A google app script to pull data from the harvest api",
	"main": "index.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@google/clasp": "^2.4.2"
	},
	"devDependencies": {
		"@types/google-apps-script": "^1.0.77",
		"@types/node": "^20.9.0",
		"prettier": "^3.0.3"
	}
}
